# page-replacement-simulator
An interactive web-based simulator that demonstrates FIFO, LRU, and Optimal page replacement algorithms used in virtual memory management. The application visualizes memory frame allocation, tracks page hits and faults, and compares algorithm performance using dynamic tables and charts. Built using HTML, CSS, and JavaScript.
